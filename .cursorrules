# Inner Animal Media — Cursor

**First read (all tools):** [`AGENTS.md`](AGENTS.md) · D1 `rule_platform_lockdown_engineering_law` (`trigger_type=system`)

**Platform identity:** `docs/platform/PLATFORM_CONSTITUTION.md` → `docs/workspace/WORKSPACE_CONSTITUTION.md` → `docs/products/PRODUCT_REGISTRY.md`

**Always-on Cursor helpers:** `.cursor/rules/iam-core.mdc` · `iam-ship-lanes.mdc` · `iam-ship-gate.mdc` · `iam-agent-delivery-workflow.mdc` · `iam-ticket-dual-pass-e2e.mdc`

**Ship gate:** local → commit/push → deploy **by host** (Mac: `deploy:full`/`deploy:fast`; GCP VM: `ship:remote` → CF Builds). SSOT: `docs/platform/mac-free-ship-lanes-2026-07.md`.

**No test/smoke in deploy** unless you explicitly ask (see iam-core).

**Platform law in D1:** `agentsam_rules_document` — runtime via `fetchTriggeredRulesForSystemPrompt`. LOCKED rows need `apply_mode=always` **and** `trigger_type=system` (column default is `manual` — silent no-op). Guard: `npm run guard:engineering-laws`.

**Never commit:** `.cursor/mcp.json` (developer-owned), `.env.cloudflare`, secrets, `*.bak`, `.scratch/`.
