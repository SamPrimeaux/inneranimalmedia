# Infrastructure documentation

Operational reference for the Inner Animal Media platform edge stack (Workers, tunnel, GCP PTY host).

| Doc | Contents |
|-----|----------|
| [terminal.md](./terminal.md) | Tunnel, VPC connector, `iam-pty`, auth tokens, multi-tenant terminal workspace layout, runbooks |

Sanitized for git: real IPs, tunnel IDs, project IDs, and secrets live in `.env.cloudflare` and Worker secrets only — see the security note at the top of `terminal.md`.
