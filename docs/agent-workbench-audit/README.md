# Moved

**This directory is superseded.**

Use **`docs/dashboard-agent-audit/README.md`** for the live production audit of **https://inneranimalmedia.com/dashboard/agent**.

Source of truth for UI: **`dashboard/`** only — not `agent-dashboard/`.
