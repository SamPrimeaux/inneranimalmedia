# Movie Mode — product principles

1. **Collaborative first** — humans edit; AI assists
2. **Not AI-only** — timeline, import, export are core
3. **Agent tools for heavy lanes** — render, convert, media search
4. **Standalone intent** — yes; `moviemode-service` is early extraction
5. **Shared Images/DAM** — media bin should use `cms_assets` patterns
