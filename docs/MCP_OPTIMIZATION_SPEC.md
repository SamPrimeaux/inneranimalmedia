# MCP Optimization Spec — moved

Canonical location: **[SamPrimeaux/inneranimalmedia-mcp-server → docs/MCP_OPTIMIZATION_SPEC.md](https://github.com/SamPrimeaux/inneranimalmedia-mcp-server/blob/main/docs/MCP_OPTIMIZATION_SPEC.md)**

The MCP server (`mcp.inneranimalmedia.com`) lives in its own repo; the spec was briefly committed here by mistake. Platform-side items the spec references (deploy:full ship gate, `agentsam_tool_profile_bindings.force_first_tool`, routing-arm economics) still land in this monorepo — see the spec's §5.2, §6.1, §6.2.
