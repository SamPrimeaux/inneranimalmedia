# Tomorrow — 2026-06-01 — Repo hygiene + Agent Sam tool spine

**Parent sprint:** `plan_may22_2026_agent_sam`  
**Daily plan (D1):** `plan_jun01_2026_execution`  
**Last ship:** `0f24eda` — Anthropic `temperature` omitted from Messages API bodies (Worker `dc1e8e28-…`)

---

## What happened tonight (from `wrangler tail`)

| Symptom | Cause | Tomorrow fix |
|--------|--------|----------------|
| “What tools are available” → generic Worker bindings answer | **Agent** route compiled **`finalToolCount: 0`**; model `gpt-5.4-nano` with no tools | P0: Agent/Ask routes must expose catalog tools or honest “no tools on this route” UX |
| Multitask audit → subagents “ok” but no README/plan files | RWS fanout ran; **`github_file` 404** on `agentsam_plans/README.md`; reads may not hit local PTY repo | Wire read path to workspace clone; fix requested file list |
| `agentsam_tool_call_events_run_id_fkey` | Child runs use `ar_*` ids; Supabase expects UUID `run_id` | Skip or map child telemetry (see `hyperdrive-write.js` prior fix — extend to all RWS children) |
| `rws_spawn_telemetry ok:3` | Telemetry counts success without content validation | Add “bytes read > 0” gate before marking child ok |

---

## P0 — Agent can actually work on the repo

1. **Tool spine on Agent mode** — `route_requirements_id=agent` must compile non-empty tools for file/github/d1 when policy allows (tonight: `tool_allowlist_count: 0`).
2. **Finish fs tools** — `read_file` → PTY (`executeFsReadFile`) ✅; **`list_dir` / `write_file`** still HTTP loopback to **unwired** `/api/fs/list`, `/api/fs/write` → implement or redirect to PTY.
3. **RWS read children** — Ensure `compiled_tool_names` non-empty **and** reads return bytes from `/workspace/{tenant}/{user_id}/inneranimalmedia` (not GitHub-only for local audit tasks).
4. **Supabase tool_call_events** — Stop FK errors on multitask child runs (noise + missed observability).

---

## P1 — Repo dead-code / doc hygiene (audit backlog)

High-confidence removals or archive (do **not** delete without grep + deploy smoke):

| Item | Action |
|------|--------|
| `worker.js` references in README, `docs/route-map.md`, 50+ docs | **Edit** — canonical entry is `src/index.js` only; `worker.js` **missing** from tree |
| `package.json` `main` / `start` → `server.js` | Fix or remove (file absent) |
| `src/core/legacy-worker-annotate.js` | Delete if still unimported |
| `scripts/patch_results/` + `*.bak` in tree | Archive or delete |
| Sandbox scripts (`deploy-sandbox.sh`, `promote-to-prod.sh`) | Mark historical or delete |
| Scripts with `~/Downloads/inneranimalmedia` | Delete or header “DO NOT RUN” |
| `plan_may14_2026_repair` in active docs | Replace with `plan_may22_2026_agent_sam` |
| `agent-dashboard/` paths in docs | Point to `dashboard/` |
| `docs/route-map.md` “Auto-generated from worker.js” | Regenerate from `src/index.js` or deprecate |

Full inventory: see **Dead code register** below.

---

## P2 — Product validation (30 min)

1. Dashboard **Agent** → pinned `anthropic_sonnet_4_6` → “Read `README.md` first paragraph only.”
2. **Multitask** → same prompt; confirm subagents return real excerpts in parent message.
3. **Opus** one-liner (temperature regression): `Reply OK` on `anthropic_opus_4_7`.
4. `npm run guard:identity` before any identity-touching PR.

---

## Dead code register (prioritized)

### High confidence — stale or missing

- **Missing:** `worker.js`, `server.js` (still documented / in package.json)
- **Unwired:** `/api/fs/list`, `/api/fs/read`, `/api/fs/write` (called from `src/tools/fs.js`)
- **Unused:** `src/core/legacy-worker-annotate.js`
- **Artifacts:** `scripts/patch_results/`, on-disk `src/api/*.bak`
- **Deprecated deploy:** `scripts/deploy-sandbox.sh`, `scripts/promote-to-prod.sh`, `scripts/deploy-test-promote.sh`
- **Wrong paths:** `scripts/fix-flat-dashboard-vite-deps.sh`, `scripts/patch-dashboard-flat-deploy-paths.sh`, `scripts/repair-dashboard-vite-flat.sh`, `scripts/promote-flat-dashboard-to-parent.sh`
- **Stale docs:** `docs/worker-modularization-status-2026-05-02.md`, `docs/MEGAPROMPT_AGENT_SAM_CURSOR_PARITY.md` (plan_may14), `docs/PROJECT_AUDIT_VERBATIM.md`
- **README contradictions:** `worker.js` fallback, `deploy:full` “generators and ingests first” (actual: `deploy-frontend.sh` only)

### Medium — archive / label historical

- `.agentsam_evals/` (eval battle outputs)
- `captures/inneranimalmedia/` (Playwright artifacts)
- `prototypes/inneranimalmedia-cms-editor/`
- `scripts/audit/iam_audit_report.md` (point-in-time)
- `migrations/d1_manual/*plan_may14*`
- Autorag docs titled “worker.js routing”

### Not dead (do not delete)

- `src/api/agent.js` vs `src/tools/builtin/agent.js` vs `src/api/analytics/agent.js` — different roles
- `dashboard/index-v3.html` — R2 shell fallback in `src/index.js`
- `src/email-templates/*.html` — transactional email

---

## D1 todos (registered under `plan_jun01_2026_execution`)

| todo_id | Deliverable |
|---------|-------------|
| `todo_jun01_agent_tool_spine` | Agent route `finalToolCount > 0` for standard dev tasks |
| `todo_jun01_fs_list_write_pty` | `list_dir` / `write_file` off dead HTTP loopback |
| `todo_jun01_rws_telemetry_fk` | No `agentsam_tool_call_events` FK errors on RWS children |
| `todo_jun01_readme_truth_pass` | README + top 5 docs match `src/index.js` + `deploy:full` reality |
| `todo_jun01_dead_code_archive` | Patch_results + legacy-worker-annotate + sandbox scripts handled |

---

## Commands cheat sheet

```bash
cd /Users/samprimeaux/inneranimalmedia

# Deploy (only path)
npm run deploy:full

# D1 plan / todos
./scripts/with-cloudflare-env.sh npx wrangler d1 execute inneranimalmedia-business \
  --remote -c wrangler.production.toml --file=./migrations/496_jun01_daily_execution_plan.sql

# Tail prod while testing chat
wrangler tail inneranimalmedia

# Identity guard before ship
npm run guard:identity
```

---

## Success criteria for end of day 2026-06-01

- [ ] Agent mode answers “what tools do I have?” from **compiled route tools**, not generic binding list
- [ ] Multitask repo audit returns **real file excerpts** or explicit errors (not hollow `ok:3`)
- [ ] No Supabase FK spam on `agentsam_tool_call_events` during multitask
- [ ] README “Canonical Facts” matches repo (no `worker.js`, accurate `deploy:full`)
- [ ] `plan_jun01_2026_execution` tasks_done reflects shipped commits
