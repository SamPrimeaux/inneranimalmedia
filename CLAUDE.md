# Claude Code — read this first

**Canonical engineering law (all tools):** [`AGENTS.md`](./AGENTS.md)

**Runtime twin (in-app Agent Sam):** D1 `agentsam_rules_document.id = rule_platform_lockdown_engineering_law`  
(`apply_mode=always`, `trigger_type=system` — required or the row is never injected)

Then: `docs/platform/PLATFORM_CONSTITUTION.md` → workspace/product constitutions as needed.
