[project]
name = "iam-workflows"
version = "0.1.0"
description = "Durable Agent Sam workflow orchestration — Python Workflows + handler registry dispatch"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]

[tool.pywrangler]
