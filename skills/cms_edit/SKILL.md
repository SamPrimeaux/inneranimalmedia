# CMS Edit — PrimeTech Agent Loop

Canonical copy: `docs/skills-playbooks/cms_edit/SKILL.md`

Use when `task_type` or `route_key` is `cms_edit`.

See D1 `agentsam_skill` id `skill_iam_cms_edit` for injected system prompt content.
