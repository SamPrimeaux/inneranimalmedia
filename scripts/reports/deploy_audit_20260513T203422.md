# Deploy Script Audit
Generated: 2026-05-13T20:42:14Z | Model: qwen2.5-coder:7b | 30 scripts

## Summary

| Status | Count |
|--------|-------|
| ACTIVE | 30 |

- Scripts in package.json: 43
- Scripts in agentsam_scripts D1: 0

---

## Main entrypoints

### ✅ `scripts/deploy-full.sh`
**Size:** 5KB | **In package.json:** yes | **In D1 registry:** no

**Does:** This script automates a full deployment process for a Cloudflare Workers + R2 + D1 platform, including generating route maps, building codebase priority, reingesting Supabase documents, ingesting D1 memory, deploying frontend, indexing codebase, running deploy eval, and performing post-deploy tasks.  
**Risk:** The script does not handle potential errors gracefully during the deployment process. For example, if any step fails, it will exit immediately without attempting to clean up or notify of the failure.  
**Suggestion:** Implement a more robust error handling mechanism that logs errors and attempts to clean up resources before exiting.

### ✅ `scripts/deploy-frontend.sh`
**Size:** 23KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Automates the deployment of a frontend application to R2 and updates Cloudflare Workers.  
**Risk:** The script does not handle errors gracefully when `rclone` fails, which could lead to partial deployments.  
**Suggestion:** Add error handling around the `rclone sync` command to ensure that any failures are properly logged and the deployment process is halted.

### ✅ `scripts/deploy.sh`
**Size:** 2KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Deploys a Cloudflare Worker and logs the deployment details to an external tracking system.  
**Risk:** The script does not handle authentication errors from the logging service, which could lead to failed deployments being logged as successful.  
**Suggestion:** Add error handling for the logging request to ensure that failed deployments are properly recorded.

### ✅ `scripts/with-cloudflare-env.sh`
**Size:** 1KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Loads Cloudflare environment variables and runs a command with those variables set.  
**Risk:** The script does not validate the existence of required environment variables before running commands, which could lead to errors or security issues if the variables are not set.  
**Suggestion:** Add checks for required environment variables (e.g., `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN`) and exit with an error message if any are missing.

### ✅ `scripts/deploy-gate.sh`
**Size:** 21KB | **In package.json:** no | **In D1 registry:** no

**Does:** The script performs a quality gate for deploying to Cloudflare Workers, R2, and D1. It checks the environment, runs benchmarks, audits asset hashes, and writes deployment notes to D1.  
**Risk:** The script lacks validation for the `AITESTSUITE_R2_BUCKET` variable, which could lead to unintended deployments or data loss if not properly configured.  
**Suggestion:** Add a check to ensure that `AITESTSUITE_R2_BUCKET` is set before proceeding with R2-related operations.

### ✅ `scripts/deploy-with-record.sh`
**Size:** 9KB | **In package.json:** no | **In D1 registry:** no

**Does:** Automates the deployment of a Cloudflare Worker, uploads updated files to R2, and logs deployment details in D1.  
**Risk:** The script does not handle potential errors during file uploads or database insertions, which could lead to partial deployments.  
**Suggestion:** Add error handling around file upload and database operations to ensure a complete deployment.

## Post-deploy / verification

### ✅ `scripts/post-deploy-smoke.sh`
**Size:** 2KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Runs a series of health checks and end-to-end tests for a Cloudflare Workers + R2 + D1 platform.  
**Risk:** The script relies on environment variables that may not be set or may contain sensitive information.  
**Suggestion:** Use a `.env` file to manage environment variables securely and avoid hardcoding secrets in the script.

### ✅ `scripts/post-deploy-record.sh`
**Size:** 2KB | **In package.json:** no | **In D1 registry:** no

**Does:** Records a deployment in D1 with details such as version ID, timestamp, and triggered by.  
**Risk:** SQL injection vulnerability due to direct string interpolation into the SQL command.  
**Suggestion:** Use parameterized queries to prevent SQL injection.

### ✅ `scripts/post-deploy-memory-sync.sh`
**Size:** 4KB | **In package.json:** no | **In D1 registry:** no

**Does:** This script deploys a new version of an application to Cloudflare Workers, updates a D1 database with deployment details, and inserts a record into Supabase for tracking.  
**Risk:** The script uses environment variables without validation, which could lead to security issues if these variables are not properly sanitized or set.  
**Suggestion:** Validate and sanitize all environment variables before using them in the script.

### ✅ `scripts/verify-agentsam-telemetry-after-deploy.sh`
**Size:** 4KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Executes SQL queries to retrieve recent data from a D1 database related to tool chain, usage events, command runs, and error logs, and identifies anomalies.  
**Risk:** The script does not handle potential errors gracefully beyond logging them with `|| true`, which could lead to silent failures.  
**Suggestion:** Implement proper error handling and logging for critical operations.

### ✅ `scripts/settings_deploy_check.sh`
**Size:** 3KB | **In package.json:** no | **In D1 registry:** no

**Does:** This script automates a deployment check for a Cloudflare Workers + R2 + D1 platform, including verifying git state, safety checks, building the project, and running public route checks.  
**Risk:** The script does not explicitly verify the presence of necessary environment variables or credentials required for deploying to Cloudflare Workers, R2, or D1. This could lead to deployment failures if these are missing.  
**Suggestion:** Add a step to check for the existence and validity of required environment variables before proceeding with the deployment checks.

## R2 / manifest

### ✅ `scripts/build-r2-deploy-manifest.mjs`
**Size:** 4KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Generates a deterministic deploy manifest for Cloudflare R2 storage from a local build directory.  
**Risk:** The script does not validate the `bucket` parameter, which could lead to deploying files to an incorrect or non-existent bucket.  
**Suggestion:** Add validation to ensure the specified `bucket` exists in Cloudflare R2 before proceeding with deployment.

### ✅ `scripts/reconcile-r2-deploy.mjs`
**Size:** 7KB | **In package.json:** no | **In D1 registry:** no

**Does:** Compares a local deploy manifest to R2 object inventory, activates manifest keys, and marks missing keys as stale. Does not delete R2 objects.  
**Risk:** The script does not handle errors or exceptions that may occur during the execution of SQL queries or file operations, which could lead to data corruption or loss.  
**Suggestion:** Add error handling around database operations and file I/O to ensure robustness.

### ✅ `scripts/compute-deploy-input-hash.mjs`
**Size:** 1KB | **In package.json:** no | **In D1 registry:** no

**Does:** Computes a SHA-256 hash of the files in either the 'supabase-docs' or 'migrations' directory.  
**Risk:** The script does not handle errors gracefully, such as when a file is unreadable. This could cause the script to fail silently.  
**Suggestion:** Add error handling for unreadable files and provide more informative error messages.

### ✅ `scripts/patch-dashboard-flat-deploy-paths.sh`
**Size:** 2KB | **In package.json:** no | **In D1 registry:** no

**Does:** Automates the deployment of a Cloudflare Worker dashboard by patching script paths, building the frontend, verifying built files, and checking for deploy gate availability.  
**Risk:** Potential issues with script path replacements and build commands could lead to deployment failures.  
**Suggestion:** Add logging before each major step to capture output and errors for debugging.

## Deploy ledger

### ✅ `scripts/record-supabase-deploy-start.mjs`
**Size:** 5KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Starts a deployment ledger entry in Supabase for the Cloudflare Workers + R2 + D1 platform, recording details about the deployment.  
**Risk:** The script does not handle errors gracefully when writing to the `.deploy-run-context.json` file, which could lead to incomplete or corrupted data if the write operation fails.  
**Suggestion:** Add error handling around the `writeFileSync` call to ensure that any issues during file writing are caught and logged appropriately.

### ✅ `scripts/record-supabase-deploy-complete.mjs`
**Size:** 6KB | **In package.json:** yes | **In D1 registry:** no

**Does:** The script finalizes a deployment by updating the build deploy event, workflow status, and inserting evaluation results into R2 and D1.  
**Risk:** The script does not handle errors gracefully when reading files or making API requests. This could lead to partial updates or data loss if an error occurs during execution.  
**Suggestion:** Implement proper error handling for file operations and API requests, ensuring that all changes are committed atomically or rolled back in case of failure.

### ✅ `scripts/record-supabase-deploy-failure.mjs`
**Size:** 5KB | **In package.json:** yes | **In D1 registry:** no

**Does:** Records a deployment failure by updating ledger rows and inserting error events in Supabase.  
**Risk:** The script does not validate input arguments, which could lead to security issues or incorrect data if malicious values are provided.  
**Suggestion:** Add input validation for `--reason`, `--exit-code`, `--failed-step`, and `--error-key` to ensure they meet expected formats.

### ✅ `scripts/finalize-stale-deploy-events.mjs`
**Size:** 5KB | **In package.json:** yes | **In D1 registry:** no

**Does:** The script cancels stale Supabase build_deploy_events based on a cutoff time and logs the results.  
**Risk:** The script does not validate input parameters thoroughly, which could lead to unintended behavior or errors.  
**Suggestion:** Add validation for all required environment variables and input flags.

### ✅ `scripts/record-d1-deploy-start.mjs`
**Size:** 7KB | **In package.json:** yes | **In D1 registry:** no

**Does:** The script records the start of a deployment in D1 and updates the deployment tracking table.  
**Risk:** The script does not handle errors gracefully when interacting with R2 or D1, which could lead to partial deployments or data inconsistencies.  
**Suggestion:** Implement error handling for database operations and ensure that all resources are properly cleaned up on failure.

### ✅ `scripts/record-d1-deploy-complete.mjs`
**Size:** 12KB | **In package.json:** yes | **In D1 registry:** no

**Does:** The script finalizes a D1 deployment by updating deployment tracking and notifications, recording evaluation health, and inserting error logs if necessary.  
**Risk:** The script lacks proper error handling for database operations, which could lead to data corruption or loss.  
**Suggestion:** Implement try-catch blocks around database operations to handle errors gracefully.

### ✅ `scripts/record-d1-deploy-failure.mjs`
**Size:** 6KB | **In package.json:** yes | **In D1 registry:** no

**Does:** The script is designed to record deployment failures for Cloudflare Workers + R2 + D1 platform by inserting error logs and updating tracking records.  
**Risk:** The script does not handle potential SQL injection risks when constructing SQL queries dynamically. This could be exploited if user inputs are not properly sanitized.  
**Suggestion:** Use parameterized queries to prevent SQL injection vulnerabilities.

### ✅ `scripts/record-d1-deployment-health.mjs`
**Size:** 10KB | **In package.json:** yes | **In D1 registry:** no

**Does:** The script reads deployment evaluation results and records the health status in a D1 table.  
**Risk:** The script does not handle errors gracefully when reading or writing files, which could lead to data loss or corruption.  
**Suggestion:** Implement error handling for file operations using try-catch blocks.

## Codebase index / eval

### ✅ `scripts/index-codebase-snapshot.mjs`
**Size:** 15KB | **In package.json:** yes | **In D1 registry:** no

**Does:** The script is designed to index and process codebase snapshots using Cloudflare Workers, R2, and D1. It collects files from a repository, parses them for relevant information, and stores the data in Supabase.  
**Risk:** The script does not handle errors gracefully, which could lead to partial or failed deployments.  
**Suggestion:** Implement error handling around file reading, API requests, and database operations to ensure robustness.

### ✅ `scripts/run-deploy-eval.mjs`
**Size:** 5KB | **In package.json:** yes | **In D1 registry:** no

**Does:** This script performs a health check and logs semantic search data to R2 using Cloudflare Workers, with optional strict semantic checks.  
**Risk:** The script does not handle potential network issues or database connection failures gracefully, which could lead to incomplete or incorrect results.  
**Suggestion:** Implement error handling for network requests and database connections to ensure robustness.

## Suspected legacy

### ✅ `scripts/deploy-sandbox.sh`
**Size:** 28KB | **In package.json:** no | **In D1 registry:** no

**Does:** The script builds and deploys a Cloudflare Worker along with static assets to an R2 bucket for a sandbox environment.  
**Risk:** The script does not handle errors or exceptions gracefully, which could lead to partial deployments or data loss.  
**Suggestion:** Implement error handling around critical operations such as building the worker and uploading files to R2.

### ✅ `scripts/deploy-test-promote.sh`
**Size:** 7KB | **In package.json:** no | **In D1 registry:** no

**Does:** Automates the deployment and testing of a Cloudflare Worker application, including building React bundles, deploying to a sandbox environment, running smoke tests, promoting to production, and pushing changes to GitHub.  
**Risk:** The script does not handle potential errors during the build or deployment processes, which could lead to partial deployments or missed issues.  
**Suggestion:** Implement error handling for each step of the deployment process to ensure that any failures are caught and addressed before proceeding.

### ✅ `scripts/deploy-cf-builds.sh`
**Size:** 3KB | **In package.json:** no | **In D1 registry:** no

**Does:** Deploys a Cloudflare Worker, syncs Vite dist and shell CSS to R2 bucket.  
**Risk:** Potential race condition in job_count handling could lead to missed uploads.  
**Suggestion:** Use `wait -n` instead of manual job count management.

### ✅ `scripts/deploy-cf-builds-prod.sh`
**Size:** 7KB | **In package.json:** no | **In D1 registry:** no

**Does:** Automates the deployment of a Cloudflare Worker to production, including building assets with Vite, syncing them to R2, and recording health and deploy logs.  
**Risk:** The script does not handle errors gracefully. If any command fails, it will exit immediately due to `set -e`, which could lead to partial deployments or missed notifications.  
**Suggestion:** Add error handling around critical steps and use a more robust method for managing temporary files.

### ✅ `scripts/dev-deploy.sh`
**Size:** 1KB | **In package.json:** no | **In D1 registry:** no

**Does:** Automates deployment of a Cloudflare Workers + R2 + D1 platform based on changes detected in the codebase.  
**Risk:** The script does not handle potential errors during deployments, which could lead to partial deployments or data loss.  
**Suggestion:** Implement error handling and logging for each deployment step.

---
*Audited by qwen2.5-coder:7b via Ollama. Review before acting on suggestions.*