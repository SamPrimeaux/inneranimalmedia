/** Registry rows for curated Agent Sam shell scripts (scripts/*.sh). */
export const WORKSPACE_ID = 'ws_inneranimalmedia';

/** @typedef {{ file: string, name: string, description: string, purpose: string, requires_env: number, owner_only: number, safe_to_run: number, preferred_for?: string, notes?: string, run_before?: string, run_after?: string, never_run_with?: string }} AgentsamScriptRow */

/** @type {AgentsamScriptRow[]} */
export const SCRIPT_ROWS = [
  {
    file: 'apply-batch1-patches.sh',
    name: 'apply-batch1-patches',
    description: 'Applies batch patch files to the working tree; mutates repository files.',
    purpose: 'maintenance',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes: 'Mutates local/checkout files. Review patches before running.',
  },
  {
    file: 'batch-api-test.sh',
    name: 'batch-api-test',
    description: 'Runs batch HTTP/API checks against configured endpoints.',
    purpose: 'test',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'API regression / smoke batch',
  },
  {
    file: 'benchmark-ai-full.sh',
    name: 'benchmark-ai-full',
    description: 'Full AI benchmarking harness across models/providers.',
    purpose: 'benchmark',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'model/provider performance comparison',
  },
  {
    file: 'benchmark-all-models.sh',
    name: 'benchmark-all-models',
    description: 'Benchmarks all configured models for latency and quality.',
    purpose: 'benchmark',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'model matrix testing',
  },
  {
    file: 'benchmark-all-providers.sh',
    name: 'benchmark-all-providers',
    description: 'Runs comparative benchmarks across AI providers.',
    purpose: 'benchmark',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'provider comparison',
  },
  {
    file: 'benchmark-cost-accuracy.sh',
    name: 'benchmark-cost-accuracy',
    description: 'Measures cost vs accuracy tradeoffs for model usage.',
    purpose: 'benchmark',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'cost/accuracy analysis',
  },
  {
    file: 'benchmark-full.sh',
    name: 'benchmark-full',
    description: 'End-to-end benchmark suite (may invoke production APIs; long-running).',
    purpose: 'benchmark',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes: 'Heavy workload; may bill APIs. Run only with explicit approval.',
  },
  {
    file: 'benchmark-providers.sh',
    name: 'benchmark-providers',
    description: 'Provider-focused latency and availability benchmarks.',
    purpose: 'benchmark',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'provider performance testing',
  },
  {
    file: 'compare-openai.sh',
    name: 'compare-openai',
    description: 'Compares OpenAI model outputs or endpoints for evaluation.',
    purpose: 'benchmark',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'OpenAI comparison runs',
  },
  {
    file: 'deploy-cf-builds-prod.sh',
    name: 'deploy-cf-builds-prod',
    description: 'Deploy path aligned with Cloudflare Builds production pipeline.',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Deploy script — requires explicit approval. Prefer npm run deploy:full:safe when R2 reconcile should stay disabled.',
  },
  {
    file: 'deploy-cf-builds.sh',
    name: 'deploy-cf-builds',
    description: 'Cloudflare Builds-oriented deploy helper.',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Deploy script — requires explicit approval. Prefer npm run deploy:full:safe when R2 reconcile should stay disabled.',
  },
  {
    file: 'deploy-frontend.sh',
    name: 'deploy-frontend',
    description: 'Builds/uploads dashboard to R2 and deploys the Worker (production path).',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Deploy script — requires explicit approval. Prefer npm run deploy:full:safe when R2 reconcile should stay disabled.',
  },
  {
    file: 'deploy-gate.sh',
    name: 'deploy-gate',
    description: 'Pre-deploy checks / gate before promotion.',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Deploy-related gate — requires explicit approval. Prefer npm run deploy:full:safe when R2 reconcile should stay disabled.',
  },
  {
    file: 'deploy-sandbox.sh',
    name: 'deploy-sandbox',
    description: 'Sandbox dashboard/worker deploy (legacy CI path — repo marks discontinued).',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Sandbox deploy script — dangerous/discontinued per repo rules; requires explicit approval.',
  },
  {
    file: 'deploy-test-promote.sh',
    name: 'deploy-test-promote',
    description: 'Test promotion flow between environments.',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Deploy script — requires explicit approval. Prefer npm run deploy:full:safe when R2 reconcile should stay disabled.',
  },
  {
    file: 'deploy-with-record.sh',
    name: 'deploy-with-record',
    description: 'Wrangler deploy with optional D1/cloudflare deployment recording.',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Deploy script — requires explicit approval. Prefer npm run deploy:full:safe when R2 reconcile should stay disabled.',
  },
  {
    file: 'deploy.sh',
    name: 'deploy',
    description: 'Worker-only deploy via wrangler (see repo deploy docs).',
    purpose: 'deploy',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes:
      'Deploy script — requires explicit approval. Prefer npm run deploy:full:safe when R2 reconcile should stay disabled.',
  },
  {
    file: 'e2e-overnight.sh',
    name: 'e2e-overnight',
    description: 'Long-running end-to-end validation suite (overnight).',
    purpose: 'test',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes: 'Long-running and potentially expensive; schedule deliberately.',
  },
  {
    file: 'iam-model-test.sh',
    name: 'iam-model-test',
    description: 'Inner Animal Media model routing / inference smoke test.',
    purpose: 'test',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'model smoke validation',
  },
  {
    file: 'iam-welcome.sh',
    name: 'iam-welcome',
    description: 'Interactive terminal splash / welcome banner for developers.',
    purpose: 'dev',
    requires_env: 0,
    owner_only: 0,
    safe_to_run: 1,
    notes: 'Prints UI only when sourced in interactive shells; no secrets required.',
  },
  {
    file: 'log-repo-edit-to-cicd-events.sh',
    name: 'log-repo-edit-to-cicd-events',
    description: 'Logs repository edit metadata into CI/CD event streams (audit trail).',
    purpose: 'audit',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    notes: 'Writes audit metadata; does not mutate application source by itself.',
  },
  {
    file: 'model-smoke-test.sh',
    name: 'model-smoke-test',
    description: 'Quick smoke test against configured model endpoints.',
    purpose: 'test',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 1,
    preferred_for: 'model smoke',
  },
  {
    file: 'populate-autorag-bucket.sh',
    name: 'populate-autorag-bucket',
    description: 'Uploads or seeds AutoRAG bucket content in R2.',
    purpose: 'ingest',
    requires_env: 1,
    owner_only: 1,
    safe_to_run: 0,
    notes: 'Mutates R2 / AutoRAG objects — verify idempotency and scope before running.',
  },
];
