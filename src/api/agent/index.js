/**
 * Agent Sam modular surface (P0-D). Main HTTP handler remains in ../agent.js until tranche 2.
 */
export {
  classifyIntent,
  inferIntentHeuristically,
  messageHasBrowserUrlNavigation,
} from './classify-intent.js';
