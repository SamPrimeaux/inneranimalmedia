
======================================================================
  T001 — oauth.js circular import + upsertOauthToken
======================================================================

[oauth.js top imports]

--- /Users/samprimeaux/inneranimalmedia/src/api/oauth.js:1-30 ---
    1  /**
    2   * OAuth + API Key connection endpoints
    3   *
    4   * Pattern:
    5   *  - GET  /api/oauth/:provider/start    -> 302 to provider (login flows when unauthenticated for google/github match worker.js)
    6   *  - GET  /api/oauth/:provider/callback (provider redirect) -> 302 back to return_to (default: /dashboard/overview; integrations flows pass explicit return_to)
    7   *  - POST /api/oauth/apikey/:provider   (auth required) -> validate + store encrypted
    8   *
    9   * Notes:
   10   *  - Uses SESSION_CACHE KV for state storage (10m TTL).
   11   *  - Persists tokens in D1 `user_oauth_tokens` with forward-compatible encrypted columns.
   12   */
   13  import { getAuthUser, jsonResponse } from '../core/auth.js';
   14  import {
   15    handleGoogleLoginOAuthCallback,
   16    handleGitHubLoginOAuthCallback,
   17  } from './oauth-login-callbacks.js';
   18  import { getAESKey, aesGcmEncryptToB64, aesGcmDecryptFromB64 } from '../core/crypto-vault.js';
   19  import { syncProviderModels } from './integrations/model-sync.js';
   20  
   21  const OAUTH_STATE_TTL_SECONDS = 600;
   22  
   23  /**
   24   * Supabase Management API OAuth only (https://api.supabase.com).
   25   * Must NOT reuse env.SUPABASE_OAUTH_* — those belong to the project's Auth OAuth Server (login) in auth.js.
   26   */
   27  function getSupabaseManagementOAuthCredentials(env) {
   28    const id =
   29      typeof env.SUPABASE_MANAGEMENT_OAUTH_CLIENT_ID === 'string'
   30        ? env.SUPABASE_MANAGEMENT_OAUTH_CLIENT_ID.trim()

[oauth-login-callbacks.js top imports]

--- /Users/samprimeaux/inneranimalmedia/src/api/oauth-login-callbacks.js:1-25 ---
    1  /**
    2   * Login OAuth callbacks (Google / GitHub) — parity with worker.js
    3   * handleGoogleOAuthCallback / handleGitHubOAuthCallback.
    4   *
    5   * Integration OAuth stays in oauth.js (oauth_state_* + user_id payload).
    6   */
    7  import { getAuthUser, resolveTenantAtLogin, createLoginSession } from '../core/auth.js';
    8  import { provisionAuthenticatedUser } from '../core/provisionAuthenticatedUser.js';
    9  import { ensureAppUser } from '../core/ensureAppUser.js';
   10  import { upsertOauthToken } from './oauth.js';
   11  
   12  function oauthOrigin(url) {
   13    return url.origin || 'https://inneranimalmedia.com';
   14  }
   15  
   16  const DASHBOARD_LOGIN_FALLBACK = '/dashboard/overview';
   17  
   18  /**
   19   * Path (pathname + search) for same-origin post-OAuth app login redirect.
   20   * Never send users to Integrations from a social **login** callback.
   21   */
   22  function safeDashboardLoginRedirectPath(originBase, returnTo) {
   23    if (!returnTo || typeof returnTo !== 'string') return DASHBOARD_LOGIN_FALLBACK;
   24    const t = returnTo.trim();
   25    if (!t) return DASHBOARD_LOGIN_FALLBACK;

[upsertOauthToken occurrences in oauth.js]

  line 292:
  >>>   292  export async function upsertOauthToken(

  line 654:
  >>>   654      await upsertOauthToken(env, {

  line 1401:
  >>>  1401          await upsertOauthToken(env, {

  line 1417:
  >>>  1417          await upsertOauthToken(env, {

  line 1432:
  >>>  1432          await upsertOauthToken(env, {

  line 1473:
  >>>  1473          await upsertOauthToken(env, {

  line 1561:
  >>>  1561        await upsertOauthToken(env, {

[upsertOauthToken FULL BODY — lines 292 to 293]

--- /Users/samprimeaux/inneranimalmedia/src/api/oauth.js:292-293 ---
  292  export async function upsertOauthToken(
  293    env,

[private helpers called INSIDE upsertOauthToken — looking for non-imported fns]

  line 212:
        211  
  >>>   212  async function pragmaColumns(DB, tableName) {
        213    const out = await DB.prepare(`PRAGMA table_info(${tableName})`).all();

  line 219:
        218  
  >>>   219  export async function ensureOauthTokenColumns(DB) {
        220    const cols = await pragmaColumns(DB, 'user_oauth_tokens');

  line 220:
        219  export async function ensureOauthTokenColumns(DB) {
  >>>   220    const cols = await pragmaColumns(DB, 'user_oauth_tokens');
        221    const alters = [];

  line 239:
        238    }
  >>>   239    return await pragmaColumns(DB, 'user_oauth_tokens');
        240  }

  line 301:
        300  
  >>>   301    const cols = await ensureOauthTokenColumns(env.DB); // PRAGMA requirement before write
        302    const createdAt = nowSeconds();

  line 763:
        762    const tenantId = authUser?.tenant_id || '';
  >>>   763    await ensureOauthTokenColumns(env.DB); // PRAGMA before write
        764    const encrypted = await encryptWithVault(env, apiKey);

  line 768:
        767    // Store under provider key; keep account_identifier empty.
  >>>   768    const cols = await pragmaColumns(env.DB, 'user_oauth_tokens');
        769    const hasEncrypted = cols.has('access_token_encrypted');

  line 1528:
       1527    if (!env?.DB || !userId || !env.VAULT_MASTER_KEY) return null;
  >>>  1528    await ensureOauthTokenColumns(env.DB);
       1529    const acct = supabaseOAuthAccountIdentifier(workspaceId);

[what upsertOauthToken calls that live in oauth.js (not imported)]
  line    27  getSupabaseManagementOAuthCredentials
  line    41  oauthClientIdTail
  line    50  nowSeconds
  line    54  oauthStateKey
  line    62  integrationOAuthKvKey
  line    68  kvPutIntegrationOAuthState
  line    76  kvGetIntegrationOAuthState
  line    87  kvDeleteIntegrationOAuthState
  line    94  safeReturnTo
  line   102  oauthLoginOrigin
  line   109  loginGoogleOAuthStart
  line   148  loginGitHubOAuthStart
  line   176  encryptWithVault
  line   181  decryptWithVault
  line   186  resolveOAuthAccessToken
  line   199  resolveOAuthRefreshToken
  line   212  pragmaColumns
  line   219  ensureOauthTokenColumns
  line   242  normalizeProvider
  line   248  mapTokenProviderForStorage
  line   257  supabaseManagementOAuthRedirectUri
  line   268  integrationUserId
  line   273  supabaseOAuthAccountIdentifier
  line   278  fetchSupabaseManagementProjects
  line   292  upsertOauthToken
  line   413  githubAuthUrl
  line   427  googleAuthUrl
  line   448  cloudflareAuthUrl
  line   474  supabaseAuthUrl
  line   501  exchangeGithub
  line   517  githubAccount
  line   526  exchangeGoogle
  line   543  googleUserinfo
  line   557  gmailOauthStateKvKey
  line   561  googleClientSecretForGmailCallback
  line   569  exchangeGoogleAuthCodeForGmail
  line   592  gmailOAuthStart
  line   616  gmailOAuthCallback
  line   674  exchangeCloudflare
  line   694  exchangeSupabase
  line   715  validateApiKey
  line   758  storeApiKeyAsOauth
  line   822  mcpOAuthNow
  line   826  mcpOAuthBase64Url
  line   833  mcpOAuthSha256Hex
  line   838  mcpOAuthPkceS256
  line   843  mcpOAuthRandomToken
  line   850  mcpOAuthJsonError
  line   854  mcpOAuthSafePathWithSearch
  line   858  mcpOAuthAllowedRedirectUri
  line   892  mcpOAuthResolveTenantId
  line   908  mcpOAuthResolveWorkspaceId
  line   933  mcpOAuthNormalizeScope
  line   943  mcpOAuthReadBody
  line   968  handleMcpOAuthAuthorize
  line  1040  handleMcpOAuthToken
  line  1148  handleMcpOAuthUserinfo
  line  1214  handleOAuthApi
  line  1504  refreshSupabaseAccessToken
  line  1526  getUserSupabaseToken

[circular: oauth-login-callbacks.js imports from oauth.js]

  line 10:
  >>>    10  import { upsertOauthToken } from './oauth.js';

[circular: oauth.js imports from oauth-login-callbacks.js]

  line 17:
  >>>    17  } from './oauth-login-callbacks.js';

======================================================================
  T002 — provider string mismatch: google vs google_drive in INSERT writes
======================================================================

[ALL user_oauth_tokens INSERT/UPDATE in oauth.js with provider value]

  --- INSERT/UPDATE block at line 324 ---
    324      INSERT OR REPLACE INTO user_oauth_tokens
    325        (user_id, tenant_id, person_uuid, provider, account_identifier,
    326         ${hasPlain ? 'access_token,' : ''} ${cols.has('refresh_token') ? 'refresh_token,' : ''}
    327         ${hasEncrypted ? 'access_token_encrypted, refresh_token_encrypted,' : ''}
    328         ${cols.has('scope') ? 'scope,' : ''} ${cols.has('scopes') ? 'scopes,' : ''}
    329         expires_at,
    330         ${cols.has('workspace_id') ? 'workspace_id,' : ''}
    331         ${cols.has('metadata_json') ? 'metadata_json,' : ''}
    332         ${cols.has('account_email') ? 'account_email,' : ''} ${cols.has('account_display') ? 'account_display,' : ''}
    333         ${cols.has('created_at') ? 'created_at,' : ''} ${cols.has('updated_at') ? 'updated_at,' : ''}
    334         created_at
    335        )
    336      VALUES (
    337        ?, ?, ?, ?, ?,
    338        ${hasPlain ? '?,' : ''} ${cols.has('refresh_token') ? '?,' : ''}
    339        ${hasEncrypted ? '?, ?,': ''}
    340        ${cols.has('scope') ? '?,' : ''} ${cols.has('scopes') ? '?,' : ''}
    341        ?,
    342        ${cols.has('workspace_id') ? '?,' : ''}
    343        ${cols.has('metadata_json') ? '?,' : ''}
    344        ${cols.has('account_email') ? '?,' : ''} ${cols.has('account_display') ? '?,' : ''}
    345        ${cols.has('created_at') ? '?,' : ''} ${cols.has('updated_at') ? '?,' : ''}
    346        ?
    347      )
    348    `.replace(/\s+/g, ' ').trim();

  --- INSERT/UPDATE block at line 773 ---
    773      INSERT OR REPLACE INTO user_oauth_tokens
    774        (user_id, tenant_id, provider, account_identifier,
    775         ${hasPlain ? 'access_token,' : ''}
    776         ${hasEncrypted ? 'access_token_encrypted,' : ''}
    777         ${cols.has('created_at') ? 'created_at,' : ''} ${cols.has('updated_at') ? 'updated_at,' : ''}
    778         created_at
    779        )
    780      VALUES (?, ?, ?, ?,
    781              ${hasPlain ? '?,' : ''}
    782              ${hasEncrypted ? '?,' : ''}
    783              ${cols.has('created_at') ? '?,' : ''} ${cols.has('updated_at') ? '?,' : ''}
    784              ?
    785      )
    786    `.replace(/\s+/g, ' ').trim();

[provider value written in oauth.js — look for google vs google_drive]

  line 1274:
       1272      // Login/sign-up OAuth (no session): same behavior as worker.js handleGoogleOAuthStart / handleGitHubOAuthStart.
       1273      // Callbacks: oauth-login-callbacks.js + integration branch below (/api/oauth/*/callback).
  >>>  1274      if (!authUser && provider === 'google') {
       1275        return loginGoogleOAuthStart(request, url, env);
       1276      }

  line 1325:
       1323      let redirectUrl = null;
       1324      if (provider === 'github') redirectUrl = githubAuthUrl(env, state, oauthScopes);
  >>>  1325      if (provider === 'google') redirectUrl = googleAuthUrl(env, state, oauthScopes);
       1326      if (provider === 'cloudflare') redirectUrl = cloudflareAuthUrl(env, state, oauthScopes);
       1327      if (provider === 'supabase') redirectUrl = supabaseAuthUrl(env, request, state, oauthScopes, redirectUriMgmt);

  line 1347:
       1345      const origin = new URL(request.url).origin;
       1346      if (!state || !code) {
  >>>  1347        if (provider === 'google' || provider === 'github') {
       1348          return Response.redirect(`${origin}/auth/login?error=missing`, 302);
       1349        }

  line 1366:
       1364  
       1365      // Google: integration KV payload has user_id; login payload has redirectUri (same key prefix oauth_state_*).
  >>>  1366      if (provider === 'google') {
       1367        const raw = await env.SESSION_CACHE.get(oauthStateKey(state));
       1368        if (!raw) return new Response(null, { status: 404 });

  line 1414:
       1412            account_display: acct.login ? `github.com/${acct.login}` : null,
       1413          });
  >>>  1414        } else if (provider === 'google') {
       1415          const tok = await exchangeGoogle(env, code);
       1416          const info = await googleUserinfo(tok.access_token);

  line 1421:
       1419            tenant_id: tenantId,
       1420            person_uuid: personUuid,
  >>>  1421            provider: 'google',
       1422            access_token: tok.access_token,
       1423            refresh_token: tok.refresh_token || null,

[provider value in oauth-login-callbacks.js for comparison]

  line 471:
        469        user_id: driveUserId,
        470        tenant_id: driveTenantId,
  >>>   471        provider: 'google_drive',
        472        account_identifier: oauthEmail,
        473        access_token: tokens.access_token,

  line 485:
        483        .run();
        484      return new Response(
  >>>   485        `<script>window.opener?.postMessage({type:'oauth_success',provider:'google'},window.location.origin);window.close();</script>`,
        486        { headers: { 'Content-Type': 'text/html' } },
        487      );

[user-oauth-token.js alias map]

  line 28:
         27  
  >>>    28  /** Map API aliases to provider column values in user_oauth_tokens. */
         29  function mapIncomingProvider(provider) {

  line 32:
         31    const p = raw.toLowerCase();
  >>>    32    if (p === 'google' || p === 'gdrive' || p === 'google_drive') return 'google_drive';
         33    if (p === 'github') return 'github';

  line 37:
         36  
  >>>    37  function googleOAuthClientSecret(env) {
         38    const a = typeof env?.GOOGLE_OAUTH_CLIENT_SECRET === 'string' ? env.GOOGLE_OAUTH_CLIENT_SECRET.trim() : '';

  line 44:
         43  
  >>>    44  /** Google OAuth integrations that use oauth2.googleapis.com refresh with a refresh_token. */
         45  function isGoogleOAuthRefreshProvider(provider) {

  line 47:
         46    const p = String(provider || '').toLowerCase();
  >>>    47    if (p === 'google_drive' || p === 'gmail' || p === 'google_gmail' || p === 'google_calendar') return true;
         48    if (p.startsWith('google_')) return true;

  line 48:
         47    if (p === 'google_drive' || p === 'gmail' || p === 'google_gmail' || p === 'google_calendar') return true;
  >>>    48    if (p.startsWith('google_')) return true;
         49    return false;

  line 134:
        133  export async function refreshGoogleToken(env, userId, provider, refreshToken, row) {
  >>>   134    const clientSecret = googleOAuthClientSecret(env);
        135    if (!env?.GOOGLE_CLIENT_ID || !clientSecret || !refreshToken) return null;

  line 137:
        136  
  >>>   137    const res = await fetch('https://oauth2.googleapis.com/token', {
        138      method: 'POST',

======================================================================
  T003 — execute-approved-tool insertion point in agent.js
======================================================================

[how agent.js does path routing — find the pattern]

  line 7161:
       7159    const identity = await resolveIdentity(env, request);
       7160    const ingestChatBypass =
  >>>  7161      path === '/api/agent/chat' &&
       7162      method === 'POST' &&
       7163      isIngestSecretAuthorized(request, env);

  line 9142:
       9140  
       9141    // ── /api/agent/chat ───────────────────────────────────────────────────────
  >>>  9142    if (path === '/api/agent/chat' && method === 'POST') {
       9143      const ingestBypass = isIngestSecretAuthorized(request, env);
       9144      return agentChatSseHandler(env, request, ctx, { ingestBypass, identity });

[lines around /api/agent/chat main handler]

--- /Users/samprimeaux/inneranimalmedia/src/api/agent.js:3154-3164 ---
 3154                ? String(agentToolLedger.runId)
 3155                : sessionId,
 3156          run_group_id: chatAgentRunId != null ? String(chatAgentRunId) : null,
 3157          route_path: '/api/agent/chat',
 3158          model_key: modelKey,
 3159        });
 3160        if (grModel.blocked) {
 3161          throw new Error(`GUARDRAIL_BLOCKED:${grModel.decision?.reason || 'model_blocked'}`);
 3162        }
 3163        // Provider resolved inside dispatchStream from agentsam_ai.api_platform (Workers AI → OAI-shaped SSE).
 3164        stream = await dispatchStream(env, request, {

[dispatchToolCall signature — line 1704 area]

--- /Users/samprimeaux/inneranimalmedia/src/api/agent.js:1700-1720 ---
 1700      agentsamToolsId: rk.agentsam_tools_id ?? null,
 1701    };
 1702  }
 1703  
 1704  async function dispatchToolCall(env, toolName, input, context = {}) {
 1705    const t0 = Date.now();
 1706    const cached = await tryReadAgentsamToolCache(env, {
 1707      workspaceId: context.workspaceId,
 1708      tenantId: context.tenantId,
 1709      toolName,
 1710      toolInput: input,
 1711    });
 1712    if (cached.hit) return cached.value;
 1713  
 1714    const sess = {
 1715      user_id: context.userId,
 1716      workspace_id: context.workspaceId,
 1717      workspaceId: context.workspaceId,
 1718      tenant_id: context.tenantId,
 1719      session_id: context.sessionId,
 1720      person_uuid: context.personUuid,

[dispatchToolCallWithBudget call pattern — how context is built]

--- /Users/samprimeaux/inneranimalmedia/src/api/agent.js:3855-3880 ---
 3855        emit('tool_start', {
 3856          tool_name: call.name,
 3857          input_preview: JSON.stringify(call.input || {}).slice(0, 200),
 3858        });
 3859        let toolRows = null;
 3860        const toolBudgetMs = resolveToolExecutionBudgetMs(call.name, call.input);
 3861        try {
 3862          const execResult = await dispatchToolCallWithBudget(
 3863            env,
 3864            call.name,
 3865            call.input,
 3866            {
 3867              sessionId,
 3868              tenantId,
 3869              userId,
 3870              workspaceId,
 3871              personUuid: mcpCtx.personUuid,
 3872              isSuperadmin: mcpCtx.isSuperadmin,
 3873              request,
 3874            },
 3875            toolBudgetMs,
 3876          );
 3877          if (execResult && typeof execResult === 'object') {
 3878            if (Array.isArray(execResult.rows)) toolRows = execResult.rows;
 3879            else if (Array.isArray(execResult.results)) toolRows = execResult.results;
 3880          }

[how nearby routes load auth/session — find getAuthUser pattern near chat]

  line 37:
         36  import { jsonResponse }                                 from '../core/responses.js';
  >>>    37  import { getAuthUser, getSession,
         38           isIngestSecretAuthorized,

  line 475:
        474    try {
  >>>   475      const authUser = await getAuthUser(request, env).catch(() => null);
        476      const wr = await resolveEffectiveWorkspaceId(env, request, authUser, cache || {});

  line 4727:
       4726    const tenantId = panel.tenantId != null ? String(panel.tenantId).trim() : '';
  >>>  4727    const userId = panel.userId != null ? String(panel.userId).trim() : '';
       4728    const workspaceId = panel.workspaceId != null ? String(panel.workspaceId).trim() : '';

  line 4994:
       4993    const tenantId = opts.tenantId ?? null;
  >>>  4994    const userId = opts.userId ?? null;
       4995    const sessionId =

  line 5159:
       5158    const headerWorkspaceId = request.headers.get('x-iam-workspace-id');
  >>>  5159    const resolvedWorkspaceId =
       5160      (headerWorkspaceId != null && String(headerWorkspaceId).trim() !== ''

  line 5189:
       5188      userId: session?.user_id,
  >>>  5189      workspaceId: resolvedWorkspaceId,
       5190      tenantId: session?.tenant_id ?? null,

  line 5226:
       5225    const actorCtx = await resolveIamActorContext(request, env).catch(() => null);
  >>>  5226    const authUser = ingestBypass ? null : await getAuthUser(request, env).catch(() => null);
       5227  

  line 5238:
       5237    }
  >>>  5238    const userId =
       5239      session?.user_id ||
  ... (56 more hits)

[execute-approved-tool in ChatAssistant — what body it sends]

  line 982:
        974          if (q.length > 0) {
        975            const next = q[0];
        976            setMessageQueue((prev) => prev.slice(1));
        977            void handleSendRef.current(next);
        978          }
        979          return;
        980        }
        981  
  >>>   982        const res = await fetch('/api/agent/chat/execute-approved-tool', {
        983          method: 'POST',
        984          headers: { 'Content-Type': 'application/json' },
        985          body: JSON.stringify({
        986            tool_name: tool.name,
        987            tool_input: tool.parameters ?? {},
        988            conversation_id: conversationId || undefined,
        989          }),
        990        });

  line 1007:
        999          const next = [...prev];
       1000          const last = next[next.length - 1];
       1001          if (last?.role === 'assistant') {
       1002            next[next.length - 1] = { ...last, content: last.content + suffix };
       1003          }
       1004          return next;
       1005        });
       1006      } catch (e) {
  >>>  1007        console.error('[ChatAssistant] execute-approved-tool', e);
       1008        setPendingToolApproval(null);
       1009        const msg = e instanceof Error ? e.message : String(e);
       1010        setMessages((prev) => {
       1011          const next = [...prev];
       1012          const last = next[next.length - 1];
       1013          if (last?.role === 'assistant') {
       1014            next[next.length - 1] = { ...last, content: `${last.content}\n\n[Approve request failed: ${msg}]` };
       1015          }

[plan-task/resume handler as reference for how to build a similar route]

--- /Users/samprimeaux/inneranimalmedia/src/api/agent.js:8782-8830 ---
 8782    // ── POST /api/agent/plan-task/resume — one plan terminal task after Allow (SSE) ──
 8783    if (path === '/api/agent/plan-task/resume' && method === 'POST') {
 8784      const authUser = await getAuthUser(request, env);
 8785      if (!authUser) return jsonResponse({ error: 'Unauthorized' }, 401);
 8786      if (!env.DB) return jsonResponse({ error: 'DB not configured' }, 503);
 8787      const body = await request.json().catch(() => ({}));
 8788      const planId = String(body.plan_id ?? body.planId ?? '').trim();
 8789      const taskId = String(body.task_id ?? body.taskId ?? '').trim();
 8790      const commandRunIdIn = String(body.command_run_id ?? body.commandRunId ?? '').trim();
 8791      const approvalId = String(body.approval_id ?? body.approvalId ?? '').trim();
 8792      if (!planId || !taskId || !approvalId) {
 8793        return jsonResponse({ error: 'plan_id, task_id, and approval_id required' }, 400);
 8794      }
 8795  
 8796      const wsRes = await resolveEffectiveWorkspaceId(env, request, authUser, {}).catch(() => null);
 8797      const workspaceId =
 8798        (authUser.active_workspace_id != null && String(authUser.active_workspace_id).trim() !== ''
 8799          ? String(authUser.active_workspace_id).trim()
 8800          : null) ||
 8801        (wsRes && !wsRes.error && wsRes.workspaceId ? String(wsRes.workspaceId).trim() : null);
 8802      if (!workspaceId) return jsonResponse({ error: 'no_workspace', redirect: '/onboarding' }, 403);
 8803  
 8804      let tenantId =
 8805        authUser.tenant_id != null && String(authUser.tenant_id).trim() !== ''
 8806          ? String(authUser.tenant_id).trim()
 8807          : null;
 8808      if (!tenantId) tenantId = await fetchAuthUserTenantId(env, authUser.id);
 8809  
 8810      const uid = String(authUser.id || '').trim();
 8811      const canon = await resolveCanonicalUserId(uid, env).catch(() => uid);
 8812  
 8813      const qRow = await env.DB
 8814        .prepare(
 8815          `SELECT q.id, q.status, q.command_run_id, q.user_id, q.expires_at, q.execution_step_id
 8816           FROM agentsam_approval_queue q
 8817           WHERE q.id = ? AND lower(q.status) = 'approved'
 8818             AND (q.expires_at IS NULL OR q.expires_at > unixepoch())`,
 8819        )
 8820        .bind(approvalId)
 8821        .first()
 8822        .catch(() => null);
 8823  
 8824      if (!qRow?.id) {
 8825        return jsonResponse({ error: 'approval_not_verified', message: 'Approve this task first (Allow).' }, 403);
 8826      }
 8827  
 8828      const qCrid = qRow.command_run_id != null ? String(qRow.command_run_id).trim() : '';
 8829      if (commandRunIdIn && qCrid && qCrid !== commandRunIdIn) {
 8830        return jsonResponse({ error: 'command_run_mismatch' }, 400);

======================================================================
  T004/T005 — ActiveFile type definition + FormData send location
======================================================================

[where ActiveFile type is defined]

  line 106:
        104  
        105  /** Open file in Monaco — local handle, R2, GitHub, or Drive metadata */
  >>>   106  export type ActiveFile = {
        107    name: string;
        108    content: string;
  FOUND in dashboard/types.ts

[handle field on ActiveFile or tree node]

  line 132:
        131  
  >>>   132  async function persistNativeDirectoryHandle(handle: FileSystemDirectoryHandle): Promise<void> {
        133    const db = await openNativeWsDb();

  line 185:
        184      kind: 'file' | 'directory';
  >>>   185      handle: any; // FileSystemHandle
        186      children?: FileNode[];

[where openFile / openInEditorFromExplorer is called with file metadata]

[ChatAssistant.tsx — where FormData is built and appended before fetch]

--- /Users/samprimeaux/inneranimalmedia/dashboard/features/agent-chat/ChatAssistant.tsx:1167-1172 ---
 1167      })();
 1168  
 1169      const effectiveConvId = conversationId || (() => { const id = crypto.randomUUID(); setConversationId(id); try { localStorage.setItem(LS_AGENT_CHAT_CONVERSATION_ID, id); } catch (_) {} return id; })();
 1170      const form = new FormData();
 1171      form.append('message', messageForApi);
 1172      form.append('mode', mode);

  line 1170:
       1169      const effectiveConvId = conversationId || (() => { const id = crypto.randomUUID(); setConversationId(id); try { localStorage.setItem(LS_AGENT_CHAT_CONVERSATION_ID, id); } catch (_) {} return id; })();
  >>>  1170      const form = new FormData();
       1171      form.append('message', messageForApi);

  line 1171:
       1170      const form = new FormData();
  >>>  1171      form.append('message', messageForApi);
       1172      form.append('mode', mode);

  line 1172:
       1171      form.append('message', messageForApi);
  >>>  1172      form.append('mode', mode);
       1173      form.append('agent_mode', mode);

  line 1173:
       1172      form.append('mode', mode);
  >>>  1173      form.append('agent_mode', mode);
       1174      form.append('runtime_intent_mode', mode);

  line 1174:
       1173      form.append('agent_mode', mode);
  >>>  1174      form.append('runtime_intent_mode', mode);
       1175      form.append('model', effectiveModelKey);

  line 1175:
       1174      form.append('runtime_intent_mode', mode);
  >>>  1175      form.append('model', effectiveModelKey);
       1176      const selectedModelProvider = chatModels.find((m) => m.model_key === effectiveModelKey)?.provider || 'anthropic';

  line 1177:
       1176      const selectedModelProvider = chatModels.find((m) => m.model_key === effectiveModelKey)?.provider || 'anthropic';
  >>>  1177      form.append('provider', selectedModelProvider);
       1178      form.append('conversationId', effectiveConvId);

  line 1178:
       1177      form.append('provider', selectedModelProvider);
  >>>  1178      form.append('conversationId', effectiveConvId);
       1179      form.append('contextMode', String(activeProject));

  line 1179:
       1178      form.append('conversationId', effectiveConvId);
  >>>  1179      form.append('contextMode', String(activeProject));
       1180      if (effectiveWsId) form.append('workspace_id', effectiveWsId);

  line 1180:
       1179      form.append('contextMode', String(activeProject));
  >>>  1180      if (effectiveWsId) form.append('workspace_id', effectiveWsId);
       1181      try {

  line 1189:
       1188        }
  >>>  1189        form.append('browserContext', JSON.stringify(browserCtxPayload));
       1190      } catch {

  line 1193:
       1192      }
  >>>  1193      attachments.forEach((a) => form.append('files', a.file));
       1194  

[what agent.js reads from chat body re: file context]

  line 2957:
       2955    const path =
       2956      (typeof parsed.path === 'string' && parsed.path.trim()) ||
  >>>  2957      (typeof parsed.file_path === 'string' && parsed.file_path.trim()) ||
       2958      (typeof parsed.file === 'string' && parsed.file.trim()) ||
       2959      (Array.isArray(parsed.files_touched) && typeof parsed.files_touched[0] === 'string'

  line 7998:
       7996      const workerName = projectIdFromEnv(env) || 'unknown';
       7997      try {
  >>>  7998        const row = await env.DB.prepare(`SELECT d.git_hash, d.version, d.timestamp, g.repo_full_name, g.default_branch FROM deployments d LEFT JOIN github_repositories g ON g.cloudflare_worker_name = ? WHERE d.worker_name = ? AND d.status = 'success' ORDER BY d.timestamp DESC LIMIT 1`).bind(workerName, workerName).first();
       7999        return jsonResponse({ branch: row?.default_branch || 'main', git_hash: row?.git_hash || null, worker_name: workerName, repo_full_name: row?.repo_full_name || null, sync_last_at: row?.timestamp || null });
       8000      } catch (e) { return jsonResponse({ error: e?.message }, 500); }

  line 8015:
       8013      const repoRow = await env.DB.prepare(
       8014        `SELECT repo_full_name, default_branch
  >>>  8015         FROM github_repositories
       8016         WHERE cloudflare_worker_name = ?
       8017         LIMIT 1`,

  line 8086:
       8084      const linkedRows = await env.DB.prepare(
       8085        `SELECT repo_full_name, cloudflare_worker_name
  >>>  8086         FROM github_repositories
       8087         WHERE tenant_id = ? AND workspace_id = ?`,
       8088      )

======================================================================
  T006 — MonacoEditorView hasDiffData section + handleSaveFile signature
======================================================================

[hasDiffData block — full surrounding JSX]

  line 395:
        387    const showMediaPreview = !isEditableTextKind(resolvedKind);
        388    const previewUrl =
        389      activeFile.previewUrl ||
        390      (activeFile.localObjectUrl ? activeFile.localObjectUrl : null) ||
        391      (activeFile.r2Bucket && activeFile.r2Key
        392        ? buildR2ObjectUrl(activeFile.r2Bucket, activeFile.r2Key)
        393        : null);
        394  
  >>>   395    const hasDiffData = activeFile?.originalContent !== undefined && activeFile.originalContent !== activeFile.content;
        396  
        397    return (
        398      <div className="flex flex-col h-full w-full bg-[var(--scene-bg)] overflow-hidden">
        399        {/* ── Tab Header ── */}
        400        <div className="h-9 flex items-center border-b border-[var(--dashboard-border)] bg-[var(--dashboard-panel)] shrink-0 overflow-x-auto no-scrollbar">
        401          {tabs.map((tab) => (
        402            <div
        403              key={tab.id}

  line 433:
        425        {/* ── Editor Toolbar (Active Tab Stats) ── */}
        426        <div className="h-8 flex items-center justify-between px-3 bg-[var(--scene-bg)] border-b border-[var(--dashboard-border)] shrink-0">
        427          <div className="flex items-center gap-4 text-[10px] uppercase tracking-widest text-[var(--text-muted)] font-bold">
        428             <span>{language}</span>
        429             <span className="opacity-50">UTF-8</span>
        430             {gitActionHint && <span className="text-[var(--solar-cyan)] animate-pulse">{gitActionHint}</span>}
        431          </div>
        432          <div className="flex items-center gap-2">
  >>>   433             {hasDiffData && (
        434               <button onClick={() => setShowDiff(!showDiff)} className={`px-2 py-0.5 rounded border transition-all ${showDiff ? 'bg-[var(--solar-cyan)] text-black border-[var(--solar-cyan)]' : 'border-[var(--dashboard-border)]'}`}>
        435                 Diff
        436               </button>
        437             )}
        438             {isDirty && (
        439               <button 
        440                  onClick={() => onSave?.(activeFile.content)}
        441                  className="px-3 py-0.5 bg-[var(--solar-cyan)] text-black rounded font-bold shadow-[0_0_10px_rgba(45,212,191,0.2)]"

  line 471:
        463                        URL.revokeObjectURL(activeFile.localObjectUrl!);
        464                      } catch {
        465                        /* ignore */
        466                      }
        467                    }
        468                  : undefined
        469              }
        470            />
  >>>   471          ) : showDiff && hasDiffData ? (
        472            <DiffEditor
        473              height="100%"
        474              language={language}
        475              theme="meauxcad-dark"
        476              original={activeFile.originalContent ?? ''}
        477              modified={activeFile.content}
        478              options={{ ...EDITOR_OPTIONS, readOnly: false }}
        479            />

[DiffEditor render — exact lines]

  line 2:
          1  import React, { useEffect, useRef, useState, useCallback } from 'react';
  >>>     2  import Editor, { DiffEditor, useMonaco } from '@monaco-editor/react';
          3  import {
          4    Save,
          5    GitCompare,
          6    Copy,
          7    Check,

  line 170:
        165    const isDirty = activeFile?.isDirty;
        166  
        167    const monaco = useMonaco();
        168    const editorRef = useRef<any>(null);
        169    const isThemeReady = useRef(false);
  >>>   170    const [showDiff, setShowDiff] = useState(false);
        171    const [copied, setCopied] = useState(false);
        172    const [gitActionHint, setGitActionHint] = useState<string | null>(null);
        173  
        174    // Custom theme from :root CSS vars
        175    useEffect(() => {

  line 434:
        429             <span className="opacity-50">UTF-8</span>
        430             {gitActionHint && <span className="text-[var(--solar-cyan)] animate-pulse">{gitActionHint}</span>}
        431          </div>
        432          <div className="flex items-center gap-2">
        433             {hasDiffData && (
  >>>   434               <button onClick={() => setShowDiff(!showDiff)} className={`px-2 py-0.5 rounded border transition-all ${showDiff ? 'bg-[var(--solar-cyan)] text-black border-[var(--solar-cyan)]' : 'border-[var(--dashboard-border)]'}`}>
        435                 Diff
        436               </button>
        437             )}
        438             {isDirty && (
        439               <button 

  line 471:
        466                      }
        467                    }
        468                  : undefined
        469              }
        470            />
  >>>   471          ) : showDiff && hasDiffData ? (
        472            <DiffEditor
        473              height="100%"
        474              language={language}
        475              theme="meauxcad-dark"
        476              original={activeFile.originalContent ?? ''}

  line 472:
        467                    }
        468                  : undefined
        469              }
        470            />
        471          ) : showDiff && hasDiffData ? (
  >>>   472            <DiffEditor
        473              height="100%"
        474              language={language}
        475              theme="meauxcad-dark"
        476              original={activeFile.originalContent ?? ''}
        477              modified={activeFile.content}

[handleSaveFile — full signature + what it accepts]

  line 2131:
       2128      return map[ext] || 'text/plain; charset=utf-8';
       2129    };
       2130  
  >>>  2131    const handleSaveFile = useCallback(async (content: string) => {
       2132      if (!activeFile) return;
       2133      if (typeof activeFile.workspacePath === 'string' && activeFile.workspacePath.startsWith('mcp_tool:')) {
       2134        const toolId = activeFile.workspacePath.slice('mcp_tool:'.length).trim();

  line 2978:
       2975                            <MonacoEditorView
       2976                                fileData={activeFile}
       2977                                isDirty={isDirty}
  >>>  2978                                onSave={handleSaveFile}
       2979                                onCursorPositionChange={(line, col) => setCursorPos({ line, col })}
       2980                                onEditorModelMeta={setEditorMeta}
       2981                                onChange={(val) => {

[how onSave is passed to MonacoEditorView]

  line 21:
         18  import { WorkspaceLauncher } from './components/WorkspaceLauncher';
         19  import { XTermShell, XTermShellHandle } from './components/XTermShell';
         20  import { ExtensionsPanel } from './components/ExtensionsPanel';
  >>>    21  import { MonacoEditorView, type EditorModelMeta } from './components/MonacoEditorView';
         22  import { LocalExplorer } from './components/LocalExplorer';
         23  import { BrowserView } from './components/BrowserView';
         24  import { StatusBar, type AgentNotificationRow } from './components/StatusBar';

  line 2975:
       2972  
       2973                    {activeTab === 'code' && (
       2974                        <div className="absolute inset-0 z-10">
  >>>  2975                            <MonacoEditorView
       2976                                fileData={activeFile}
       2977                                isDirty={isDirty}
       2978                                onSave={handleSaveFile}

  line 2978:
       2975                            <MonacoEditorView
       2976                                fileData={activeFile}
       2977                                isDirty={isDirty}
  >>>  2978                                onSave={handleSaveFile}
       2979                                onCursorPositionChange={(line, col) => setCursorPos({ line, col })}
       2980                                onEditorModelMeta={setEditorMeta}
       2981                                onChange={(val) => {

[MonacoEditorView props interface]

--- /Users/samprimeaux/inneranimalmedia/dashboard/components/MonacoEditorView.tsx:1-60 ---
    1  import React, { useEffect, useRef, useState, useCallback } from 'react';
    2  import Editor, { DiffEditor, useMonaco } from '@monaco-editor/react';
    3  import {
    4    Save,
    5    GitCompare,
    6    Copy,
    7    Check,
    8    FileCode2,
    9    RotateCcw,
   10    GitBranch,
   11  } from 'lucide-react';
   12  
   13  import type { ActiveFile } from '../types';
   14  import { useEditor } from '../src/EditorContext';
   15  import { FilePreview } from '../src/components/FilePreview';
   16  import { detectFileKind, isEditableTextKind } from '../src/lib/fileKind';
   17  import { buildR2ObjectUrl } from '../src/lib/mediaPreview';
   18  import { X } from 'lucide-react';
   19  
   20  type FileData = ActiveFile;
   21  
   22  const IMAGE_EXTS = ['.png', '.jpg', '.jpeg', '.gif', '.webp', '.svg', '.bmp', '.ico', '.avif'];
   23  const isImageKey = (key: string) => IMAGE_EXTS.some((e) => key.toLowerCase().endsWith(e));
   24  const isImageMime = (ct: string) => ct.trim().toLowerCase().startsWith('image/');
   25  
   26  export type EditorModelMeta = {
   27    tabSize: number;
   28    insertSpaces: boolean;
   29    eol: 'LF' | 'CRLF';
   30    encoding: string;
   31  };
   32  
   33  interface MonacoEditorViewProps {
   34    fileData: FileData | null;
   35    onChange?: (val?: string) => void;
   36    onSave?: (content: string) => void;
   37    isDirty?: boolean;
   38    /** Live cursor for status bar (IDE parity). */
   39    onCursorPositionChange?: (line: number, column: number) => void;
   40    /** Indent / EOL / encoding for status bar (from the live Monaco model). */
   41    onEditorModelMeta?: (meta: EditorModelMeta) => void;
   42  }
   43  
   44  const LANG_MAP: Record<string, string> = {
   45    ts: 'typescript', tsx: 'typescript',
   46    js: 'javascript', jsx: 'javascript', mjs: 'javascript', cjs: 'javascript',
   47    json: 'json', jsonc: 'json',
   48    css: 'css', scss: 'scss', less: 'less',
   49    html: 'html', htm: 'html',
   50    md: 'markdown', mdx: 'markdown',
   51    py: 'python',
   52    sh: 'shell', bash: 'shell', zsh: 'shell',
   53    toml: 'toml',
   54    yaml: 'yaml', yml: 'yaml',
   55    go: 'go',
   56    rs: 'rust',
   57    sql: 'sql',
   58    pgsql: 'sql',
   59    graphql: 'graphql', gql: 'graphql',
   60    env: 'plaintext',

======================================================================
  T007 — change_sets migration schema
======================================================================
  No migration file found — searching...

[163_agentsam_cursor_parity.sql]
-- =============================================================================
-- 163_agentsam_cursor_parity.sql — idempotent CREATE (inneranimalmedia-business)
-- =============================================================================
-- Production (cf87b717-d4e2-4cf8-bab0-a81268e32d49): applied before 2026-03-22 audit.
-- Safe to run on new/staging D1: all CREATE TABLE IF NOT EXISTS.
-- Original review copy: scripts/d1-cursor-parity-schema-review.sql (keep in sync).
-- =============================================================================
-- Purpose: Cursor-parity agentsam_* tables + optional theme INSERTs (commented).
--          On existing prod, creates are no-ops; uncomment Section K only if themes missing.
--
-- Already in this repo (migrations — you likely HAVE these):
--   agent_sessions, agent_messages, agent_configs, agent_execution_plans,
--   agent_request_queue, agent_command_proposals, agent_telemetry,
--   mcp_tool_calls, mcp_usage_log, mcp_workflows, terminal_sessions,
--   user_settings, user_workspace_settings, cms_themes, ai_compiled_context_cache,
--   governance_*, change_sets, playwright_jobs, cloudflare_deployments, ...
--
-- This file ADDS (or proposes) IAM / Agent Sam policy + indexing + runs + themes.
-- Naming prefix: agentsam_*  (and one agentsam_ table set from docs/AGENTSAM_IGNORE_AND_RULES.md)
--
-- After review: split into migrations/16x_*.sql and run with wrangler d1 execute.
-- =============================================================================


-- -----------------------------------------------------------------------------
-- SECTION A — Optional: cms_themes picker metadata (uncomment if columns missing)
-- -----------------------------------------------------------------------------
-- PRAGMA table_info(cms_themes);
-- If theme_family / sort_order do not exist:

-- ALTER TABLE cms_themes ADD COLUMN theme_family TEXT DEFAULT 'custom';
--    -- suggested values: dark | light | high_contrast_dark | high_contrast_light | custom
-- ALTER TABLE cms_themes ADD COLUMN sort_order INTEGER DEFAULT 100;

-- CREATE INDEX IF NOT EXISTS idx_cms_themes_family_sort
--   ON cms_themes(theme_family, sort_order);


-- -----------------------------------------------------------------------------
-- SECTION B — Agent Sam user policy (Cursor “Agents” settings plane)
-- -----------------------------------------------------------------------------
-- workspace_id: use '' (empty string) for user-global default; non-empty = override for that workspace.

CREATE TABLE IF NOT EXISTS agentsam_user_policy (
  user_id TEXT NOT NULL,
  workspace_id TEXT NOT NULL DEFAULT '',
  auto_run_mode TEXT NOT NULL DEFAULT 'allowlist',
  browser_protection INTEGER NOT NULL DEFAULT 0,
  mcp_tools_protection INTEGER NOT NULL DEFAULT 1,
  file_deletion_protection INTEGER NOT NULL DEFAULT 1,
  external_file_protection INTEGER NOT NULL DEFAULT 1,
  default_agent_location TEXT DEFAULT 'pane',
  text_size TEXT DEFAULT 'default',
  auto

======================================================================
  T008 — playwright_jobs INSERT statement
======================================================================

[INSERT INTO playwright_jobs — full statement]

--- /Users/samprimeaux/inneranimalmedia/src/integrations/playwright.js:115-137 ---
  115  
  116          try {
  117              await env.DB.prepare(
  118                  `INSERT INTO playwright_jobs (id, job_type, url, status, metadata, user_id, workspace_id, created_at)
  119                   VALUES (?, 'screenshot', ?, 'pending', ?, ?, ?, datetime('now'))`,
  120              )
  121                  .bind(
  122                      jobId,
  123                      targetUrl,
  124                      JSON.stringify({ source: 'dashboard_browser_tab' }),
  125                      authUser.id,
  126                      workspaceId,
  127                  )
  128                  .run();
  129          } catch (e) {
  130              const msg = String(e?.message || e);
  131              if (msg.includes('user_id') || msg.includes('workspace_id')) {
  132                  return jsonResponse(
  133                      {
  134                          error: 'playwright_jobs schema missing user_id',
  135                          detail: msg,
  136                          hint: 'Apply migrations/281_playwright_jobs_user_workspace.sql to D1 (remote)',
  137                      },

[metadata column — what currently goes in]

  line 118:
        116          try {
        117              await env.DB.prepare(
  >>>   118                  `INSERT INTO playwright_jobs (id, job_type, url, status, metadata, user_id, workspace_id, created_at)
        119                   VALUES (?, 'screenshot', ?, 'pending', ?, ?, ?, datetime('now'))`,
        120              )

======================================================================
  T009 — terminal_history INSERT + triggered_by
======================================================================

[terminal.js — INSERT INTO terminal_history full statement]

--- /Users/samprimeaux/inneranimalmedia/src/core/terminal.js:330-348 ---
  330      seq = 0;
  331    }
  332    seq += 1;
  333    await env.DB.prepare(
  334      `INSERT INTO terminal_history (id, terminal_session_id, tenant_id, sequence, direction, content, triggered_by, agent_session_id, recorded_at) VALUES (?,?,?,?,?,?,?,?,?)`
  335    ).bind('th_' + crypto.randomUUID().replace(/-/g, '').slice(0, 16), terminalSessionId, tenantId, seq, 'input', commandText.slice(0, 5000), 'agent', sessionId, now).run();
  336    seq += 1;
  337    await env.DB.prepare(
  338      `INSERT INTO terminal_history (id, terminal_session_id, tenant_id, sequence, direction, content, exit_code, triggered_by, agent_session_id, recorded_at) VALUES (?,?,?,?,?,?,?,?,?,?)`
  339    ).bind('th_' + crypto.randomUUID().replace(/-/g, '').slice(0, 16), terminalSessionId, tenantId, seq, 'output', outputText.slice(0, 10000), exitCode ?? null, 'agent', sessionId, now).run();
  340  }
  341  
  342  /**
  343   * Primary Execution Orchestrator.
  344   */
  345  export async function runTerminalCommand(env, request, command, sessionId = null, executionCtx = null) {
  346    const cmd = typeof command === 'string' ? command.trim() : '';
  347    const mode = String(executionCtx?.execution_mode || 'pty').toLowerCase();
  348    const controlTry = await runTerminalCommandViaControlPlane(env, request, cmd, mode, executionCtx || {});

[triggered_by value — what gets set today]

  line 334:
        332    seq += 1;
        333    await env.DB.prepare(
  >>>   334      `INSERT INTO terminal_history (id, terminal_session_id, tenant_id, sequence, direction, content, triggered_by, agent_session_id, recorded_at) VALUES (?,?,?,?,?,?,?,?,?)`
        335    ).bind('th_' + crypto.randomUUID().replace(/-/g, '').slice(0, 16), terminalSessionId, tenantId, seq, 'input', commandText.slice(0, 5000), 'agent', sessionId, now).run();
        336    seq += 1;

  line 338:
        336    seq += 1;
        337    await env.DB.prepare(
  >>>   338      `INSERT INTO terminal_history (id, terminal_session_id, tenant_id, sequence, direction, content, exit_code, triggered_by, agent_session_id, recorded_at) VALUES (?,?,?,?,?,?,?,?,?,?)`
        339    ).bind('th_' + crypto.randomUUID().replace(/-/g, '').slice(0, 16), terminalSessionId, tenantId, seq, 'output', outputText.slice(0, 10000), exitCode ?? null, 'agent', sessionId, now).run();
        340  }

  line 195:
        193        if (exitCode != null && direction === "output") {
        194          await this.env.DB.prepare(
  >>>   195            `INSERT INTO terminal_history (id, terminal_session_id, tenant_id, sequence, direction, content, exit_code, triggered_by, agent_session_id, recorded_at) VALUES (?,?,?,?,?,?,?,?,?,?)`,
        196          )
        197            .bind(id, this.cachedTerminalSessionId, tenantId, seq, direction, truncated, exitCode, triggeredBy, agentSid, now)

  line 201:
        199        } else {
        200          await this.env.DB.prepare(
  >>>   201            `INSERT INTO terminal_history (id, terminal_session_id, tenant_id, sequence, direction, content, triggered_by, agent_session_id, recorded_at) VALUES (?,?,?,?,?,?,?,?,?)`,
        202          )
        203            .bind(id, this.cachedTerminalSessionId, tenantId, seq, direction, truncated, triggeredBy, agentSid, now)

[runInTerminal in App.tsx — full function]

--- /Users/samprimeaux/inneranimalmedia/dashboard/App.tsx:2260-2271 ---
 2260    const runInTerminal = useCallback((cmd: string) => {
 2261      if (!isTerminalOpen) setIsTerminalOpen(true);
 2262      // Small delay to let terminal mount before writing
 2263      setTimeout(() => terminalRef.current?.runCommand(cmd), 100);
 2264    }, [isTerminalOpen]);
 2265  
 2266    const writeToTerminal = useCallback((text: string) => {
 2267      if (!isTerminalOpen) setIsTerminalOpen(true);
 2268      setTimeout(() => terminalRef.current?.writeToTerminal(text), 100);
 2269    }, [isTerminalOpen]);
 2270  
 2271    useEffect(() => {

[how runInTerminal sends the command — iam:run-in-terminal event payload]

  line 810:
        807    const [cmdHubOpen, setCmdHubOpen] = useState(false);
        808  
        809    const handleCommandExecution = useCallback((cmdText: string) => {
  >>>   810      terminalRef.current?.runCommand(cmdText);
        811      setCmdHubOpen(false);
        812    }, []);
        813  

  line 1595:
       1592        // Give terminal a frame to mount if it's currently closed
       1593        requestAnimationFrame(() => {
       1594          if (terminalRef.current) {
  >>>  1595            terminalRef.current.runCommand(d.cmd);
       1596          }
       1597        });
       1598      };

  line 2263:
       2260    const runInTerminal = useCallback((cmd: string) => {
       2261      if (!isTerminalOpen) setIsTerminalOpen(true);
       2262      // Small delay to let terminal mount before writing
  >>>  2263      setTimeout(() => terminalRef.current?.runCommand(cmd), 100);
       2264    }, [isTerminalOpen]);
       2265  
       2266    const writeToTerminal = useCallback((text: string) => {

  line 2410:
       2407                  workspaceLabel={workspaceDisplayLine}
       2408                  recentFiles={mappedRecentFiles}
       2409                  onNavigate={(nav, _q) => handleUnifiedNavigate(nav)}
  >>>  2410                  onRunCommand={(cmd) => terminalRef.current?.runCommand(cmd)}
       2411                  controlledOpen={searchOpen}
       2412                  onControlledOpenChange={onUnifiedSearchOpenChange}
       2413                  initialFacets={searchInitialFacets}

======================================================================
  T010 — agent.html route check
======================================================================

[production-dispatch.js — /dashboard/agent or agent.html references]

[index.js — agent.html or /dashboard/agent served]

[R2 asset serving logic — how HTML pages are resolved]

[agent.html size confirmation]
  EXISTS: /Users/samprimeaux/inneranimalmedia/dashboard/pages/agent.html — 86692 bytes / 2117 lines

======================================================================
  T011 — BrowserView agent open surface wiring
======================================================================

[iam:agent-open-surface — where fired and where listened]

  line 1456:
       1453          window.dispatchEvent(new CustomEvent('iam:palette-open-r2'));
       1454        }
       1455      };
  >>>  1456      window.addEventListener('iam:agent-open-surface', h as EventListener);
       1457      return () => window.removeEventListener('iam:agent-open-surface', h as EventListener);
       1458    }, [openTab, revealMainWorkspaceIfNarrow, isNarrowViewport]);
       1459  

  line 1457:
       1454        }
       1455      };
       1456      window.addEventListener('iam:agent-open-surface', h as EventListener);
  >>>  1457      return () => window.removeEventListener('iam:agent-open-surface', h as EventListener);
       1458    }, [openTab, revealMainWorkspaceIfNarrow, isNarrowViewport]);
       1459  
       1460    /** Browser / cdt_* tool activity from Agent SSE — surface the Browser tab so the workbench matches agent actions. */

[BrowserView — navigate / url change API]

  line 1038:
       1036    []);
       1037  
  >>>  1038    const navigate = useCallback(async (raw: string) => {
       1039      const s = raw.trim();
       1040      if (!s || isVirtual(s)) return;

  line 1238:
       1236              if (e.key !== 'Enter') return;
       1237              const n = normalizeUrl(inputVal);
  >>>  1238              if (n) void navigate(n);
       1239            }}
       1240            placeholder="https://"

  line 1556:
       1554        if (url) setSecondaryUrl(url);
       1555      };
  >>>  1556      window.addEventListener('iam-browser-navigate',           onPrimary);
       1557      window.addEventListener('iam-browser-navigate-secondary', onSecondary);
       1558      return () => {

  line 1557:
       1555      };
       1556      window.addEventListener('iam-browser-navigate',           onPrimary);
  >>>  1557      window.addEventListener('iam-browser-navigate-secondary', onSecondary);
       1558      return () => {
       1559        window.removeEventListener('iam-browser-navigate',           onPrimary);

  line 1559:
       1557      window.addEventListener('iam-browser-navigate-secondary', onSecondary);
       1558      return () => {
  >>>  1559        window.removeEventListener('iam-browser-navigate',           onPrimary);
       1560        window.removeEventListener('iam-browser-navigate-secondary', onSecondary);
       1561      };

  line 1560:
       1558      return () => {
       1559        window.removeEventListener('iam-browser-navigate',           onPrimary);
  >>>  1560        window.removeEventListener('iam-browser-navigate-secondary', onSecondary);
       1561      };
       1562    }, []);

  line 1660:
       1658            const msg = JSON.parse(e.data);
       1659            switch (msg.type) {
  >>>  1660              case 'navigate':
       1661                if (msg.url) setPrimaryUrl(msg.url);
       1662                break;

[what tool would fire browse_to_url — any existing browser tool]

  --- ai-dispatch.js ---

  line 38:
         37  
  >>>    38          // Dashboard self-debug friendly aliases → existing CDT / browser MCP tools (no new D1 rows).
         39          browser_open_url: 'cdt_navigate_page',

  line 39:
         38          // Dashboard self-debug friendly aliases → existing CDT / browser MCP tools (no new D1 rows).
  >>>    39          browser_open_url: 'cdt_navigate_page',
         40          browser_get_dom_summary: 'cdt_take_snapshot',

  line 40:
         39          browser_open_url: 'cdt_navigate_page',
  >>>    40          browser_get_dom_summary: 'cdt_take_snapshot',
         41          browser_click: 'cdt_click',

  line 41:
         40          browser_get_dom_summary: 'cdt_take_snapshot',
  >>>    41          browser_click: 'cdt_click',
         42          browser_type: 'cdt_fill',

  line 42:
         41          browser_click: 'cdt_click',
  >>>    42          browser_type: 'cdt_fill',
         43          browser_press: 'cdt_press_key',

  line 43:
         42          browser_type: 'cdt_fill',
  >>>    43          browser_press: 'cdt_press_key',
         44          browser_get_console_errors: 'cdt_list_console_messages',
  ... (10 more hits)

======================================================================
  AUDIT COMPLETE — paste full output back for patch generation
======================================================================

Tasks covered:
  T001 — oauth.js circular import + upsertOauthToken extraction
  T002 — provider string 'google' vs 'google_drive' write sites
  T003 — execute-approved-tool insertion point in agent.js
  T004 — fs.js source routing (uses T005 output)
  T005 — ActiveFile type + FormData send location
  T006 — MonacoEditorView hasDiffData + Accept/Reject insertion
  T007 — change_sets migration schema
  T008 — playwright_jobs INSERT + metadata column
  T009 — terminal_history INSERT + triggered_by
  T010 — agent.html route serving confirmation
  T011 — BrowserView iam:agent-open-surface wiring

