# secrets (R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY live in .env.cloudflare for local scripts only)
.env
.env.*
.env.cloudflare
!.env.example
!.env.cloudflare.example
.dev.vars
*.pem
*.key

# dependencies
node_modules/
dashboard/node_modules/

# Supabase CLI local cache (version pins, etc.)
supabase/.temp/

# build/cache/logs
.wrangler/
.worker-dist/
.cache/
tmp/
*.log

# OS/editor
.DS_Store
._*
.vscode/*
!.vscode/settings.json
.cursor/*
!.cursor/hooks.json
!.cursor/agents/
!.cursor/agents/**
!.cursor/rules/
!.cursor/rules/cloudflare*.mdc

# generated dashboard build
dashboard/dist/
dashboard/public/cms/vendor/*.js

# old nested / reference repos - do not commit
inneranimalmedia-agentsam-dashboard/
inneranimalmedia-main-repo/
gorilla-mode/
cms/agentsam-cms-editor/
# Local mirror of R2 CMS HTML (not source — use R2 / publish pipeline)
cms/ws_*/

# local migration helper
promote-deploy-root-from-child.sh

# legacy local junk / old nested repos

# local-only runtime
.tmp/

# Generated deploy manifests (local analytics — do not commit)
analytics/deploys/

# Deploy ledger scratch (Supabase record-* scripts)
.deploy-run-context.json
.deploy-worker-stats.json
.deploy-sw-tiered-manifest.json
.deploy-eval-results.json
.deploy-tool-events.jsonl
.deploy-pipeline-stats.json
.deploy-route-stats.json
.deploy-codebase-index-stats.json
# Deploy-full content-hash checkpoints (local / CI cache — skip redundant Supabase embed + D1 memory ingest)
.deploy-supabase-docs-hash
.deploy-migrations-hash

# R2 asset URL dumps (local inventory exports)
*-r2-asset-urls.txt

# Local artifacts (Playwright bundles, HAR/trace/session material, etc.)
artifacts/
playwright-report/
test-results/

# Optional local R2 upload helper for theme-debug bundles (uses production wrangler config)
scripts/upload-theme-debug-to-r2.sh

# IAM quality artifacts - stored in R2, not Git
captures/*/results/
captures/*/raw-playwright-report/
captures/*/raw-quality-report/
quality-report/
captures/*/upload-failures.log
iam_remediation.py
iam_remediation_output.txt

# Local secret audit tooling + reports (may reference env keys — never commit)
analytics/secret-audit-report.json
scripts/audit_secrets.py

# Snapshot / schema audit outputs (local only)
snapshot-before.json
snapshot-after.json
scripts/agentsam_schema_audit.json
scripts/agentsam_schema_chunks.jsonl
agentsam_inspection.txt
audit_agentsam_report.json

# Root one-off audit/patch scratch (committed tooling lives under scripts/)
/audit_*.py
/patch_*.py

# One-off local D1 migration scratch (use migrations/ for committed schema)
run_migrations.sh

# Agent Sam local embed/inspection session helpers (not shipped)
scripts/chunk_agentsam_inspection.py
scripts/consolidate_deployments.py
scripts/inspect_agentsam.py
scripts/quick_audit.py
scripts/repo_map.py
scripts/session_closeout.py

# Local deploy/test reports
iam-test-reports/
.env.rotate

.env.mcp.local
.mcp_exports.sh

__pycache__/

# Agent Sam local smoke/eval secrets
.env.agentsam.local
.env.openai-smoke.local
.env.anthropic-smoke.local

# Generated site preview output
scripts/generated-sites/
.iam_cookie
!.env.agentsam.example
analytics/codebase-index/

# Generated audit outputs
handler_type_audit_*.txt
audit_report.md
reports/*
!reports/template/
!reports/template/**
reports/.staging/

# Agent runtime state
.agents/

# Audit outputs — never commit
scripts/audit/*.json
scripts/audit/*.ndjson
scripts/audit_results/
*.audit.json
scripts/audit/auth_audit_results.json
*.bak2
*.bak
*.sql.bak
.scratch/
memory/
.deploy-r2-delta-stats.json
.zsh_history
.zsh_sessions/
executor_findings.json
inneranimalmedia-business
inneranimalmedia-business.db
/py1_*.py
/py2_*.py
*.save
