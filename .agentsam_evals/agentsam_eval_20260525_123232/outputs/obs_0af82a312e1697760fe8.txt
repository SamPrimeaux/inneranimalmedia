```json
{
  "pragma_checks": {
    "agentsam_subagent_profile": [
      "CHECK pragma(table_info('agentsam_subagent_profile')) CONTAINS expected columns and types",
      "CHECK pragma(index_list('agentsam_subagent_profile')) for required indexes"
    ],
    "agentsam_routing_arms": [
      "CHECK pragma(table_info('agentsam_routing_arms')) CONTAINS expected columns and types",
      "CHECK pragma(index_list('agentsam_routing_arms')) for required indexes"
    ],
    "agentsam_model_eval_observations": [
      "CHECK pragma(table_info('agentsam_model_eval_observations')) CONTAINS expected columns and types",
      "CHECK pragma(index_list('agentsam_model_eval_observations')) for required indexes"
    ]
  },
  "row_count_checks": {
    "agentsam_subagent_profile": "SELECT COUNT(*) FROM agentsam_subagent_profile; EXPECT count > 0",
    "agentsam_routing_arms": "SELECT COUNT(*) FROM agentsam_routing_arms; EXPECT count > 0",
    "agentsam_model_eval_observations": "SELECT COUNT(*) FROM agentsam_model_eval_observations; EXPECT count > 0"
  },
  "foreign_key_checks": {
    "agentsam_routing_arms": {
      "subagent_profile_fk": "SELECT COUNT(*) FROM agentsam_routing_arms ra LEFT JOIN agentsam_subagent_profile sp ON ra.subagent_id = sp.id WHERE sp.id IS NULL; EXPECT 0"
    },
    "agentsam_model_eval_observations": {
      "routing_arms_fk": "SELECT COUNT(*) FROM agentsam_model_eval_observations meo LEFT JOIN agentsam_routing_arms ra ON meo.routing_arm_id = ra.id WHERE ra.id IS NULL; EXPECT 0",
      "subagent_profile_fk": "SELECT COUNT(*) FROM agentsam_model_eval_observations meo LEFT JOIN agentsam_subagent_profile sp ON meo.subagent_id = sp.id WHERE sp.id IS NULL; EXPECT 0"
    }
  },
  "pass_criteria": {
    "pragma_checks": "All tables must have expected columns, correct data types and necessary indexes present",
    "row_count_checks": "All tables must contain at least one row",
    "foreign_key_checks": "No orphaned foreign keys present; all referenced ids must exist in parent tables